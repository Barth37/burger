<h1><strong>GOOD BURGER!</strong></h1>

<h2>Burgers that were tasty!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{>burgers/burgers devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are looking tasty!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{>burgers/burgers devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Tasty?</label><br>
    <input type="radio" name="devoured" value="1" checked> Yummy!<br>
    <input type="radio" name="devoured" value="0"> Was yummy
  </div>

  <button type="submit">Add Burger</button>
</form>
